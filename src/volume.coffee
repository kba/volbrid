module.exports = class Volume

	constructor: (@config) ->

	inc: (get, cb) ->
		throw "Not implemented"

	inc: (perc, cb) ->
		throw "Not implemented"

	dec: (perc, cb) ->
		throw "Not implemented"

	set: (perc, cb) ->
		throw "Not implemented"
